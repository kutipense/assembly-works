MYS	SEGMENT PARA STACK
    DW 20 DUP(?)
MYS	ENDS

MYD	SEGMENT PARA
    ARR	DW 75,85,84,65,89
    N	DW 5
MYD	ENDS

MYC	SEGMENT PARA
ASSUME CS:MYC, SS:MYS, DS:MYD

M	PROC FAR
    PUSH DS
    XOR AX,AX
    PUSH AX
    MOV AX,MYD
    MOV DS,AX

    MOV SI,1
    MOV CX,N
    DEC CX
L0: PUSH CX
    PUSH SI
    MOV CX,N
    SUB CX,SI
    XOR SI,SI
L1: MOV AX,ARR[SI]
    CMP AX,ARR[SI+2]
    JBE L2
    XCHG AX,ARR[SI+2]
    MOV ARR[SI],AX
L2: add si,2
    LOOP L1
    POP SI
    POP CX
    INC SI
    LOOP L0

    RETF
M	ENDP

MYC	ENDS
    END M
